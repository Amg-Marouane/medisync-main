CREATE DATABASE IF NOT EXISTS medisync
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

USE medisync;

-- Spring Boot / Hibernate creates and updates the tables from the JPA entities.
-- Import this file in phpMyAdmin if you want to create the database manually.
